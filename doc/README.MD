Make sure that all requirements are installed (appkernel & money).
```bash
pip install appkernel
pip intall money
pip install gevent WSGIServer
pip freeze > requirements.txt
```

docker network create service_net